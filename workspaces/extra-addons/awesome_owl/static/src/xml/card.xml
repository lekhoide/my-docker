<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2" style="width: 18rem;">
            <!-- Header có nút bấm Toggle -->
            <div class="card-header d-flex justify-content-between align-items-center bg-light">
                <h5 class="mb-0 text-primary">
                    <t t-out="props.title"/>
                </h5>
                <!-- Nút bấm sử dụng Bootstrap 5 -->
                <button 
                    class="btn btn-sm btn-outline-secondary rounded-circle" 
                    t-on-click="toggle"
                >
                    <i t-att-class="state.isOpen ? 'fa fa-chevron-up' : 'fa fa-chevron-down'"/>
                </button>
            </div>

            <!-- Nội dung Card được bao bởi t-if -->
            <div t-if="state.isOpen" class="card-body">
                <div class="card-text text-muted">
                    <t t-slot="default">
                        <span class="small italic">No content provided</span>
                    </t>
                </div>
            </div>
        </div>
    </t>
</templates>
